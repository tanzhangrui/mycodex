# V4.2 迭代纲领 — 验证增强：可配置 Verifier + 步骤级回滚

> 前情：V4.0 落地多智能体编排（Planner/Editor/Verifier，commit `4d1403f`）。
> 本轮补强编排闭环的两个已知短板。

## 短板与对策

| 短板 | 对策 |
|---|---|
| Verifier 写死 `npx tsc --noEmit`——测试驱动项目想要 `vitest run`，lint 严格项目想要 eslint | `planVerifyCommand` 进 `CodexConfig`（默认 typecheck 不变）；CLI `/plan` 从配置注入 |
| 步骤失败后该步骤的半成品编辑残留在内存 FS——用户 `/apply` 时会把带病产物写盘 | 每步执行前 `fs.createCheckpoint()`；重试耗尽 → `fs.restoreCheckpoint()` 回滚到步前状态。已完成步骤的产物保留（它们经过了验证） |

## 执行清单

| # | 项 | 文件 |
|---|---|---|
| 1 | `CodexConfig.planVerifyCommand?: string`（默认 `npx tsc --noEmit`） | `src/config/config.ts` |
| 2 | `OrchestratorOptions.verifyCommand` 注入默认验证器；每步 checkpoint + 失败回滚 | `src/core/orchestrator.ts` |
| 3 | `/plan` 传配置命令 | `src/cli/text-repl.ts` |
| 4 | 测试：失败步骤回滚（半成品撤销）/ 成功步骤不回滚 / 自定义验证命令注入 | `tests/orchestrator.test.ts` |

## 设计决策

- **失败回滚，成功保留**：失败步骤产物未过验证 = 带病；成功步骤产物已过验证 = 可信
- **回滚只动内存 FS**：磁盘状态不变，`/diff` `/apply` 信任闭环语义照旧
- **重试也回滚**：每次尝试前都取检查点（重试在失败尝试的现场上继续修——不，重试需要看到自己上次的错误现场？）

  决策：重试**不**回滚（保留上次尝试的编辑，让模型在现状上修复错误——这正是"修复"语义）；
  只有步骤最终失败（重试耗尽）才回滚到**步骤开始前**。
- 配置优先级沿用既有约定：JSON 配置文件 > 默认值

## 验收

- 双侧 typecheck 零错误 / 全量测试全绿 / 双侧打包通过 / CHANGELOG + commit + push
