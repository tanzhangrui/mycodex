# V4.4 迭代纲领 — 插件生态：示例插件 + 市场索引协议

> 前情：V4.1 已落地可依赖的插件开放协议（形状校验/去重/批量隔离）。
> 核心命题：协议有了，但没有**可运行的范例**和**可分发的发现机制**——生态起不来。
> 与 V5.0 首块合并交付。

## 交付物

| # | 项 | 说明 |
|---|---|---|
| 1 | `examples/plugins/echo-tools.mjs` | 真实可加载的示例插件（word_count + timestamp 两工具），零依赖纯 Node |
| 2 | `PLUGIN-PROTOCOL.md` | 插件作者协议文档：接口形状、导出形态、加载语义、安全边界 |
| 3 | `src/tools/marketplace.ts` | 市场索引协议：类型定义 + 容错解析 + 来源拉取（本地/HTTPS）+ 安装 |
| 4 | `examples/marketplace-index.json` | 索引格式范例 |
| 5 | 测试 | 索引解析容错 / 条目校验 / 安装（本地路径）/ 远程安全限制 |

## 市场索引协议（v1）

```json
{
  "version": 1,
  "plugins": [
    { "name": "echo-tools", "version": "1.0.0", "description": "…",
      "source": { "kind": "file", "path": "./examples/plugins/echo-tools.mjs" } }
  ]
}
```

- `source.kind`：`file`（本地/相对路径，相对索引文件解析）；`http`（HTTPS 下载，推到后续版本——远程代码执行的安全设计单独做）
- 安装语义：`file` → `registry.loadPlugin`（复用 V4.1 全部校验）；成功后建议用户把绝对路径加进 `config.plugins`

## 诚实边界

- **不做**：远程插件自动下载执行（HTTPS 下载 + 代码执行的安全模型——签名/校验和/沙箱隔离——值得单独一个版本做，草率上线 = 供应链攻击入口）
- **不做**：中心化市场服务。索引协议本身支持任意静态 JSON 托管（GitHub raw / 内网文件服务器），协议先行，服务后建

---

# V5.0 首块 — 多仓库工作区（核心层）

> 核心命题：单 workingDir 假设贯穿引擎/agent-loop/IDE——多仓库 monorepo 场景（前端仓 + 服务仓 + 共享库仓）下检索只见一仓。
> 本轮只做**核心层**（解析器 + 引擎多根索引），CLI/IDE 的多根接线推到 V5.1。

## 交付物

| # | 项 | 文件 |
|---|---|---|
| 1 | `WorkspaceResolver`：多根注册 / 名称去重 / 边界安全（越界拒绝）/ 双向路径映射 | `src/core/workspace.ts` |
| 2 | ContextEngine 多根：`index(string \| string[])`，跨根统一 `rootName/rel` 键空间 | `src/context/context-engine.ts` |
| 3 | 共享单例多根键 | 同上 |
| 4 | 测试：解析器（映射/去重/越界）+ 引擎（跨根检索/import 图跨根扩展） | `tests/workspace.test.ts` |

## 设计决策

| 决策 | 理由 |
|---|---|
| 路径键 = `rootName/相对路径` | 统一平面键空间，四路召回/import 图零改动即跨根 |
| 根名冲突自动去重（`repo` → `repo-2`） | 双仓同 basename 是常态 |
| `toWorkspaceRel` 越界返回 null + `contains` 显式判定 | 路径逃逸是安全问题不是 bug |
| CODEX.md 规则只读主根（第一个） | 每根各读一份会互相污染规则语义；多根规则合并推到 V5.1 |
| 持久化指纹 = 全根 join 后哈希 | 根集合变化 → 缓存自然失效 |

## 验收
双侧 typecheck / 全量测试全绿 / 双侧打包 / CHANGELOG（两版本）/ 一次 commit + push
