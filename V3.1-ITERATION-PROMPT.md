# Codex IDE V3.1 迭代纲领 — 四视角全面审视与顶级提示词

> 前置文档：`AI-IDE-MASTER-PROMPT.md`（产品宪法，依然有效）。
> 本文档是 V3.0 → V3.1 的差距分析与执行纲领，由"产品经理 × 架构师 × CTO × 首席科学家"四视角联合产出。

---

## 〇、审视方法

对照系：Cursor（编辑体验标杆）、Trae（VSCode 融合标杆）、Claude Code（Agent 自主性标杆）、
Cline/Roo（开源 Webview 架构标杆）。评判轴：**信任**（敢不敢让它改）、**智能**（改得对不对）、
**成本**（花多少钱）、**可靠**（失败时怎么办）、**粘性**（明天还想不想用）。

---

## 一、产品经理视角：信任与粘性缺口

| # | 缺口 | 影响 | 严重度 |
|---|---|---|---|
| P-1 | **会话刷新即丢失**：Webview 重载/VSCode 重启后对话归零 | 信任崩塌，长任务不敢用 | P0 |
| P-2 | **无空状态/新手指引**：首次打开一片空白，不知道能干嘛 | 激活流失 | P0 |
| P-3 | **无 @文件引用**：无法精确圈定上下文（Cursor 核心交互） | 廉价模型上下文弱，靠精确引用补 | P1 |
| P-4 | **无检查点回滚**：AI 改坏后只能全量拒绝或手动 git | "永远可回滚"宪法未闭环 | P1 |
| P-5 | **任务冲突即拒绝**：Agent 运行中发消息直接报错 | 打断心流 | P1 |
| P-6 | **修复选区不附带诊断**：Problems 面板的错误明明是修复的直接线索 | 修复成功率损失 | P1 |
| P-7 | 无多会话管理 / 无终端联动 / 无 Tab 补全 | 远期差异化 | P2 |

## 二、架构师视角：结构性缺口

| # | 缺口 | 决策 |
|---|---|---|
| A-1 | AgentService 单会话无队列 | 引入单槽排队（pending），运行中消息自动接续，不引入复杂并发 |
| A-2 | Host↔Webview 协议无持久化语义 | 增加 `history` 全量回放消息；webview 保持无状态渲染器 |
| A-3 | memfs 无快照/恢复能力 | 增加 `createCheckpoint/restoreCheckpoint`，回滚复用既有 apply 流 |
| A-4 | 扩展层零测试 | 纯逻辑（路由/成本/复杂度/检查点）下沉为可测函数，进根 vitest |
| A-5 | 大仓库全量快照的内存上限 | 维持现状（1MB 上限+ignore 目录已控），懒加载索引列入 V2 |

## 三、CTO 视角：工程可靠性缺口

| # | 缺口 | 决策 |
|---|---|---|
| C-1 | **openai-compatible 无重试**：廉价 API 抖动直接失败 | 429/5xx/网络错误指数退避重试 3 次（对齐 Anthropic provider） |
| C-2 | **失败后不升档**：免费模型失败=任务失败 | auto 模式下 free 档失败自动用 cheap 档重试一次（有 Key 时） |
| C-3 | 扩展侧无日志通道 | OutputChannel「Codex IDE」，错误可诊断 |
| C-4 | inline 编辑取消无效（abortController 为空） | streamOnce 自建控制器 |
| C-5 | 无 CI/发布流水线 | 列入 V2，当前以 typecheck+build+74 测试为验收门 |

## 四、首席科学家视角：智能质量缺口

| # | 缺口 | 决策 |
|---|---|---|
| S-1 | 静态复杂度启发式无反馈回路 | C-2 的失败升档即最小反馈回路；成功率统计列入 V2 |
| S-2 | 上下文无精确圈定手段 | P-3 @文件引用（敏感文件过滤，4K 字符截断，成本意识） |
| S-3 | 修复任务线索不全 | P-6 诊断注入（选中范围内的 error/warning 直接进 prompt） |
| S-4 | 弱模型工具调用可靠性 | 维持核心纯文本协议容错；系统提示针对弱模型优化列入 V2 |

---

## 五、V3.1 执行清单（本次交付 = P0/P1 全清）

### 迭代 A — 信任地基（P-1/P-2/P-5/C-3）
1. 会话持久化：消息历史 + token/成本 存 `globalState`（上限 40 条），重启自动回放
2. 欢迎页：空状态时展示品牌、三个快捷任务、模型/Key 状态引导
3. 任务排队：运行中发送 → 排队一条，当前任务结束自动接续
4. OutputChannel 日志（不含任何密钥，脱敏后输出）

### 迭代 B — 上下文工程（P-3/P-6/C-4）
5. `@相对路径` 引用：发送时解析，memfs 读取（隐私守卫生效），4K 截断注入
6. 修复选区自动附带该范围 diagnostics（error/warning）
7. inline 编辑取消修复：streamOnce 自建 AbortController

### 迭代 C — 可靠性闭环（P-4/C-1/C-2/A-3）
8. memfs 检查点：`createCheckpoint/restoreCheckpoint` + 每轮任务自动存档
9. 面板「回滚」：QuickPick 选择轮次 → 恢复 → 复用 diff/apply 流写盘
10. openai-compatible 流式重试：429/5xx/网络错误，指数退避 ×3
11. auto 模式失败升档：free 失败 → cheap 重试一次

### 验收门（每次迭代必须通过才允许 commit）
- 根 `tsc --noEmit` 零错误；`codex-ide` `tsc --noEmit` 零错误
- 根 vitest 全绿（含新增：检查点/重试/路由升级用例）
- `codex-ide` esbuild 打包成功
- commit 前 `git diff --cached` 敏感文件扫描为零匹配

### 北极星复检
- 从意图到可应用 diff 的时间 ↓（@引用减少往返；诊断注入提升一次修复率）
- 每元预算完成任务数 ↑（重试与升档只发生在失败时，不增加常态成本）
- 信任闭环完成：预览 ✓ 应用 ✓ **回滚 ✓**（本迭代补齐）
