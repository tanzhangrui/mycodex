# V4.1 迭代纲领 — 插件开放协议（强化）+ IDE 侧 /plan 入口

> 前情：V4.0/V4.2 编排闭环已落地。本轮与 V4.3 合并交付。
> 诚实边界：V1.4 已有 `loadPlugin` 与 CLI 配置接线——本轮不是"从零建插件市场"，而是**把弱协议补成可依赖的开放协议**。

## 短板与对策

| 短板 | 对策 |
|---|---|
| 协议无形状校验（只查 register 函数）——畸形插件加载后行为不可预测 | 导出 `CodexPlugin` 接口；加载时校验 name（非空字符串）/version/register |
| 同一插件重复加载 → 工具名冲突 → 整个抛错 | 已加载集合按 `name@version` 去重，重复返回 0 |
| Windows 绝对路径动态 import 失败 | `pathToFileURL` 转换 |
| 逐个加载失败会中断循环外的状态 | `loadPlugins(paths)` 批量隔离（单插件失败不拖累其他） |
| IDE 侧无 /plan 入口 | `AgentService.sendPlanned` 复用编排内核；聊天面板 `/plan ` 前缀路由 |

## 执行清单

| # | 项 | 文件 |
|---|---|---|
| 1 | `CodexPlugin` 接口 + loadPlugin 强化 + loadPlugins 批量 | `src/tools/registry.ts` |
| 2 | CLI 启动改用 loadPlugins | `src/index.ts` |
| 3 | IDE `sendPlanned`（复用 send 的初始化/成本/事件管线，计划进度以 delta 文本流式呈现——零 webview 协议改动） | `codex-ide/src/agent/agent-service.ts` |
| 4 | 聊天面板 `/plan ` 前缀路由 | `codex-ide/src/chat/chat-view-provider.ts` |
| 5 | 测试：合法插件加载执行 / 畸形插件拒绝 / 重复去重 / 批量隔离 | `tests/plugin-registry.test.ts` |

## 验收
双侧 typecheck / 全量测试全绿 / 双侧打包 / CHANGELOG / commit + push（与 V4.3 合并一次交付）
