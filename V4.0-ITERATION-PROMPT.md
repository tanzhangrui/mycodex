# V4.0 迭代纲领 — 多智能体编排（Planner / Editor / Verifier）

> 前情：V3.4 完成语义检索 × 索引持久化 × 共享内核（commit `9588f61`）。
> 本轮对照 Claude Code 的计划-执行模式与 Cursor 的多步任务编排，核心命题：
> **廉价模型单循环容易"一口气做完然后走偏"——用编排把大任务拆成小步，每步验证，错了当场反馈重试。**

## 定位与诚实边界

- 全部角色（Planner/Editor/Verifier 的执行者）由**同一廉价模型**驱动，区别只在系统提示词与职责上下文——不引入额外 API 成本
- Verifier 不是模型角色：是**确定性命令**（typecheck），模型只负责读错误、修代码
- 插件市场**不在本轮范围**（推到 V4.1），本轮只做编排闭环

## 架构

```
用户任务（/plan 前缀 或 启发式判定复杂）
  │
  ▼
Planner（单次廉价模型调用，纯文本 JSON 输出）
  │  解析失败 → 降级：直接走原单循环 runAgentLoop（绝不阻断）
  ▼
┌─ 步骤 i ──────────────────────────────┐
│ Editor：runAgentLoop（步骤作用域消息） │
│   ↓                                   │
│ Verifier：npx tsc --noEmit（沙箱）    │
│   失败 → 错误反馈消息 → 重试 1 次     │
│   仍失败 → 标记 failed，终止后续步骤  │
└───────────────────────────────────────┘
  │
  ▼
汇总（步骤状态 + 全部工具调用 + token 用量）
```

## 执行清单

| # | 项 | 文件 |
|---|---|---|
| 1 | Planner：系统提示词（严格 JSON）+ 容错解析（代码块提取/前后缀剥离/形状校验/步数上限 8）+ `createTaskPlan`（provider.stream 单次调用） | `src/core/planner.ts` |
| 2 | Orchestrator：`looksComplex` 启发式 + `runPlannedTask`（逐步执行/验证/重试/降级）；复用 `ExecutionPlan` 类型与 `runAgentLoop` 内核 | `src/core/orchestrator.ts` |
| 3 | CLI 入口：`/plan <任务>` 显式命令（不做隐式触发——成本与惊喜不可接受） | `src/cli/text-repl.ts` |
| 4 | 测试：解析容错（裸 JSON/围栏/散文包裹/垃圾→null/步数裁剪）+ 编排闭环（成功/验证失败重试/重试耗尽终止/解析失败降级） | `tests/orchestrator.test.ts` |

## 设计决策

| 决策 | 理由 |
|---|---|
| 计划解析失败 → 静默降级单循环 | 廉价模型 JSON 输出不可靠是常态，降级路径必须是一等公民 |
| Verifier 用 typecheck 而非模型评审 | 确定性信号零成本；模型评审廉价模型自己评自己 = 假验证 |
| 步骤失败重试 1 次后终止（不跳过继续） | 后续步骤依赖前序产物，带病执行 = 浪费 token 产出错误结果 |
| 编辑写入内存 FS（复用 /apply 信任闭环） | 计划执行中途不落盘，用户 /diff /apply 掌控最终写盘 |
| 显式 `/plan` 入口，不做隐式触发 | 启发式误判 = 多花一次规划调用的钱 + 交互不可预期 |

## 验收

- 双侧 typecheck 零错误
- 全量测试全绿（151 + 新增）
- CLI / 扩展打包通过
- CHANGELOG 记录 + commit + push
