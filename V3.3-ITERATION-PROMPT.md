# Codex IDE V3.3 迭代纲领 — 本地补全与终端自然语言化

> 前置文档：`AI-IDE-MASTER-PROMPT.md`（产品宪法）、`V3.2-ITERATION-PROMPT.md`。
> 本文档是 V3.2 → V3.3 的差距分析与执行纲领。

---

## 〇、审视方法

V3.2 完成上下文引擎（符号 + 依赖图 + 懒加载）。本轮交付路线图 V3.3 两项：
**本地 FIM Tab 补全**（对标 Copilot/Continue 的核心体验，但完全本地零成本）与
**终端命令自然语言化**（对标 GitHub Copilot CLI / Warp AI）。

评判轴：**零成本**（不走任何付费 API）、**零外发**（隐私宪法）、**流畅**（6G 显存 4050 无压力）。

---

## 一、产品经理视角

| # | 缺口 | 影响 | 严重度 |
|---|---|---|---|
| P-1 | 无 Tab 补全——这是 AI IDE 的高频刚需体验，缺失即"玩具" | 日常粘性 | P0 |
| P-2 | 补全若走云端 API：按键级请求 = 成本失控 + 代码外发 | 违反零成本与隐私双宪法 | P0（反向约束） |
| P-3 | 记不住命令语法，频繁切浏览器查 | 心流打断 | P1 |

## 二、架构师视角

| # | 决策 |
|---|---|
| A-1 | FIM 走 Ollama `/api/generate` + qwen2.5-coder FIM 模板（`<|fim_begin|>prefix<|fim_hole|>suffix<|fim_end|>`），1.5b 模型 6G 显存流畅 |
| A-2 | 纯逻辑与 vscode 依赖分离：`fim-core.ts`（可测）/ `fim-provider.ts`（vscode 包装），延续 presets.ts 测试模式 |
| A-3 | 防抖（默认 400ms）+ 单飞行请求（新请求 abort 旧请求），避免按键风暴压垮本地推理 |
| A-4 | Ollama 不可达静默降级：熔断 5 分钟，不打扰编辑（无弹窗），OutputChannel 记录一次 |
| A-5 | NL→终端命令复用 AgentService.streamOnce（廉价模型通道），不新建 provider 栈 |

## 三、CTO 视角

| # | 决策 |
|---|---|
| C-1 | 触发门槛：当前行光标前 ≥3 个非空白字符才发起，空行/短前缀不请求 |
| C-2 | 上下文裁剪：前缀 ≤64 行、后缀 ≤32 行、补全长 ≤96 token（num_predict） |
| C-3 | NL 命令执行前必须经用户确认（预填输入框，可编辑可取消）——命令是高危通道 |
| C-4 | 响应解析容错：FIM 输出去模板残留；NL 命令提取首个代码块或首行非空文本 |

## 四、首席科学家视角

| # | 决策 |
|---|---|
| S-1 | FIM 双侧上下文（prefix+suffix）优于纯前缀续写——中缀补全是代码场景主流形态 |
| S-2 | NL→命令 prompt 注入平台（win32/PowerShell）与工作目录上下文，提升一次准确率 |
| S-3 | 补全结果为空/纯空白不返回 item，避免幽灵建议 |

---

## 五、执行清单

1. `codex-ide/src/completion/fim-core.ts` — FIM prompt 构建 / 响应清洗 / 触发判定（零 vscode 依赖）
2. `codex-ide/src/completion/fim-provider.ts` — InlineCompletionItemProvider + Ollama 客户端 + 防抖竞态 + 熔断
3. `codex-ide/src/terminal/nl-command-core.ts` — NL→命令 prompt 构建 / 响应解析（零 vscode 依赖）
4. `codex-ide/src/terminal/nl-command.ts` — 命令面板入口（输入框 → 模型 → 预填确认 → 发送到终端）
5. `package.json` — 配置项（fim.enabled/url/model/debounceMs/上下文上限）+ 命令 + `Ctrl+Alt+T` 键位
6. `tests/ide-completion.test.ts` — 纯逻辑直测
7. 验收门：双侧 typecheck + 全量测试 + 双侧打包 → commit

## 六、明确不做

- 不做云端补全通道（成本+隐私双红线），哪怕用户有 API Key
- 不做多行 Tab 模式切换 / 部分接受（VSCode 原生 ghost text 已够，进阶交互列 V4）
- 不做终端命令的自动执行——确认环节永久保留
