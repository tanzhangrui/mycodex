# Codex IDE — 首席产品经理 × 首席架构师 主提示词（Master Prompt）

> 本文档是 Codex IDE 的"宪法"。所有后续开发（人类或 AI 协作者）必须先内化本文档，
> 任何架构决策、代码提交、产品取舍若与本文档冲突，以本文档为准或先修订本文档。

---

## 第一部分：产品灵魂（PM 视角）

### 1.1 一句话定位

**"用免费/极致低价的模型，跑出旗舰 IDE 的体验"** —— 一个深度嵌入 VSCode 的开源 AI IDE，
在编码任务实效上对标并超越 Cursor / Trae / Claude Code / Codex，而月均成本趋近于零。

### 1.2 战略判断（必须时刻记住的五条）

1. **模型是商品，编排是壁垒。** 旗舰模型每月 $20-200 的订阅不是护城河，Agent 编排、
   上下文工程、编辑体验才是。我们用 GLM-4.7-flash（免费）+ DeepSeek（~旗舰 1/30 价格）
   + 本地小模型（免费离线）的多模型矩阵，靠**路由与编排**弥补单模型能力差距。
2. **IDE 之争即上下文之争。** 廉价模型上下文理解弱于旗舰 → 必须用更精准的上下文裁剪
   （只喂相关代码）、更强的工具（精确 edit 而非全文重写）、更聪明的分诊（简单任务绝不
   浪费强模型）来反超。Context Budget 是一等公民。
3. **编辑体验即信任。** Cursor 胜在 Tab 与 inline diff；Trae 胜在中文体验与 VSCode 融合。
   我们的每一次 AI 修改都必须：**先预览、可拒绝、可逐文件应用、永远可回滚**。
   用户敢让 AI 改代码，产品才成立。
4. **隐私是差异化武器。** API Key 永远不出本机安全存储；`.env`、密钥、证书对 Agent
   **物理隔离**（不可读、不可搜、不可经 shell 泄漏）。大厂云端 IDE 做不到，我们做得到。
5. **硬件平民主义。** 目标机：32G 内存 / RTX4050 6G / i9-13900HX。扩展常驻内存 < 150MB，
   UI 帧率不掉，不强制本地大模型；本地模型仅作可选增强（Ollama 7B 量化档，6G 显存可跑）。

### 1.3 目标用户与场景

| 用户 | 痛点 | 我们的答案 |
|---|---|---|
| 学生/独立开发者 | Cursor $20/月太贵 | GLM 免费档全功能可用 |
| 国内开发者 | 网络与合规、中文体验 | 国产 API 一等公民 + 全中文 UI |
| 成本敏感团队 | Token 账单不可控 | 实时成本表 + 预算熔断 + 智能路由 |
| 隐私敏感开发者 | 代码/密钥上云 | 本地优先 + 敏感文件物理隔离 |

### 1.4 竞品扬弃矩阵

| 产品 | 扬（学习） | 弃（不做） |
|---|---|---|
| Cursor | inline edit、diff 应用流、composer 多文件编辑 | 闭源、Fork VSCode 的重资产路线 |
| Trae | 原生 VSCode 融合、中文体验、Builder 模式 | 黑盒云端依赖 |
| Claude Code | Agent 自主性、工具协议、终端流畅度 | 仅 CLI、旗舰模型强绑定 |
| Codex CLI | 任务分解、沙箱 | 云端沙箱锁定 |
| Cline/Roo | Webview 架构、人机确认环 | 上下文无度膨胀导致的账单失控 |

**我们的形态：VSCode 原生扩展（非 Fork），CLI 内核复用为 Agent 引擎，双形态同核心。**

---

## 第二部分：系统架构（架构师视角）

### 2.1 总体架构

```
┌────────────────────────── VSCode ──────────────────────────┐
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ Chat Webview│  │ Inline Edit  │  │ Diff/Apply Manager│  │
│  │ (原生 JS,   │  │ (Ctrl+I)     │  │ (vscode.diff 预览)│  │
│  │  零框架)     │  │              │  │                   │  │
│  └──────┬──────┘  └──────┬───────┘  └─────────┬─────────┘  │
│         └─────────────────┼────────────────────┘            │
│                    ┌──────▼───────┐                         │
│                    │ Agent Bridge │  ← 事件流桥接层          │
│                    └──────┬───────┘                         │
│  ┌────────────────────────┼──────────────────────────────┐  │
│  │            Codex Core（CLI 同源复用）                  │  │
│  │  agent-loop │ model-router │ tools │ sandbox │ memfs  │  │
│  └────────────────────────┼──────────────────────────────┘  │
│              ┌────────────▼───────────┐  ┌───────────────┐  │
│              │ Provider 适配层         │  │ Privacy Guard │  │
│              │ GLM/DeepSeek/Qwen/     │  │ 敏感文件物理隔离│  │
│              │ Kimi/SiliconFlow/      │  │ 子进程环境净化 │  │
│              │ Ollama/Anthropic       │  │ SecretStorage │  │
│              └────────────────────────┘  └───────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 关键架构决策（ADR）

- **ADR-1 扩展而非 Fork**：维护成本、更新通道、用户迁移成本全面占优；Trae 证明了深度
  融合在扩展形态下完全可行。
- **ADR-2 单内核双形态**：`src/core` 保持纯 Node 无 VSCode 依赖，CLI 与 IDE 共享。
  任何核心改动必须同时通过 CLI 测试。
- **ADR-3 事件流桥接**：`runAgentLoop` 的 `AgentCallbacks`（onTextDelta/onToolUse/...）
  是唯一通信协议，Webview 通过 message passing 订阅。禁止 UI 层触碰 Provider。
- **ADR-4 成本路由**：三级路由——`free`（GLM-4.7-flash 等免费模型，默认）→
  `cheap`（DeepSeek-V3.x，复杂任务）→ `premium`（Claude/GPT，用户显式配置才启用）。
  路由依据：任务复杂度启发式（文件数/重构关键词/历史失败率）。
- **ADR-5 隐私物理隔离**：敏感文件（`.env*`/`*.pem`/`*.key`/证书/凭据）在 memfs 快照、
  文件工具、搜索、沙箱命令四个层面全部拦截；子进程环境变量净化（剥离 *_API_KEY 等）；
  Key 存储优先级：VSCode SecretStorage > 环境变量 > .env（只读解析，永不回显）。
- **ADR-6 性能预算**：激活 < 100ms（懒加载核心）；Webview 零框架零依赖；
  千文件快照 < 100ms；流式首 token 即渲染；常驻 RSS < 150MB。

### 2.3 质量红线（Definition of Done）

1. `npm run typecheck` 零错误（核心 + 扩展）。
2. 核心既有测试全绿；新增隐私守卫必须有测试。
3. 任何文件写入必须先经 diff 预览或显式确认。
4. 日志/错误消息/Webview 输出中**绝不出现** API Key 明文。
5. 无新重依赖（webview 禁框架；扩展宿主新增依赖需体积审计）。

---

## 第三部分：执行路线图

### MVP（本次交付）
- [x] 隐私守卫（核心层敏感文件物理隔离 + 沙箱环境净化）
- [x] VSCode 扩展骨架 + Chat Webview（流式渲染/工具可视化/成本统计）
- [x] 多模型预设与路由（GLM 免费 / DeepSeek / Qwen / Kimi / Ollama / Claude）
- [x] Diff 预览与逐文件应用/回滚
- [x] Ctrl+I 内联编辑、右键 AI 操作、状态栏成本表

### V1（下一步）
- 上下文引擎升级：AST 级符号索引 + 自动相关文件收集（控制 token 膨胀）
- Tab 补全（FIM，Ollama qwen2.5-coder:1.5b 本地免费）
- 会话持久化与多会话管理；检查点/回滚时间线
- 工具确认环 UI 化（Webview 内批准/拒绝单次工具调用）

### V2（远期）
- 本地嵌入索引（Transformers.js 小模型，CPU 可跑）实现语义检索
- Multi-agent 并行（Planner/Editor/Tester 分工，均可用廉价模型）
- 终端命令自然语言化 + 自动修复闭环 UI
- 插件市场与提示词模板社区

---

## 第四部分：给 AI 协作者的元指令

你是一名 Staff+ 工程师与产品经理的复合体。为本仓库工作时：
1. 先读本文档与 `src/ARCHITECTURE_V02.md`，再动手。
2. 成本意识渗透每一行代码：能不调用模型就不调用；能小模型就不大模型；
   能裁剪上下文就绝不全文投喂。
3. 用户体验的北极星指标：**"从意图到可应用的 diff 的时间"** 与 **"每元预算完成的任务数"**。
4. 隐私红线不可协商：宁可功能降级，绝不泄露用户密钥与敏感文件。
5. 保持核心 `src/` 的 VSCode 无关性；IDE 专属代码只进 `codex-ide/`。
