# Codex IDE V3.4 迭代纲领 — 上下文进阶：语义检索 / 持久化 / @codebase

> 前置文档：`AI-IDE-MASTER-PROMPT.md`（产品宪法）、`V3.3-ITERATION-PROMPT.md`。
> 本文档是 V3.3 → V3.4 的差距分析与执行纲领。

---

## 〇、审视方法

V3.2 交付符号+依赖图+懒加载三路召回，V3.3 交付本地补全与终端 NL 化。本轮完成路线图
V3.4 三项：**轻量语义检索**、**索引持久化与增量更新**、**IDE 侧 @codebase 入口**。

对照系：Cursor @codebase（语义检索）、Claude Code（启动时索引复用）、Continue（本地嵌入）。
评判轴延续：**准**、**省**、**快**——冷启动快（持久化）、会话内新鲜（增量刷新）、
召回覆盖广（语义兜底）。

---

## 一、产品经理视角

| # | 缺口 | 影响 | 严重度 |
|---|---|---|---|
| P-1 | 关键词/符号都命不中时（用户用口语描述意图）召回为空 | "帮我看看登录逻辑"找不到 auth.ts 之外的实现 | P0 |
| P-2 | 每次进程启动都要重扫+重解析全部源码 | 大仓 CLI 冷启动与首次查询延迟高 | P1 |
| P-3 | Agent 会话中修改文件后，引擎视图停留在启动时快照 | 下一轮上下文检索到过期代码 | P0 |
| P-4 | IDE 聊天只有 @文件 精确引用，无全库检索入口 | 用户不知道引擎能力可被显式调用 | P1 |

## 二、架构师视角

| # | 决策 |
|---|---|
| A-1 | 语义检索用 **n-gram 哈希嵌入**（词 unigram/bigram + CJK 字 bigram → FNV-1a 哈希入 512 维稀疏向量 → 余弦相似度）：零依赖、纯本地、零外发；诚实定位为"轻量语义"（模糊词法），非真实 embedding 语义 |
| A-2 | 持久化落 `~/.codex/cache/context/<路径哈希>.json`（config 目录，不污染项目仓库）；只持久化符号索引 + 文件 size/mtime 指纹；加载时逐文件校验指纹，不匹配即弃 |
| A-3 | 增量刷新 `refresh()`：重扫（stat-only，便宜）→ 对比 size/mtime → 只失效变更文件的缓存（内容/符号/imports/嵌入）；符号索引置空惰性重建（未变文件命中逐文件缓存，无重读盘） |
| A-4 | 引擎单例下沉到 `context-engine.ts` 模块级（`getSharedContextEngine`）：agent-loop 与 IDE 聊天 @codebase 共享同一索引，杜绝双份内存与双份扫描 |
| A-5 | 写盘异步串行化（promise 链 + 防重入标记），不阻塞查询路径 |

## 三、CTO 视角

| # | 决策 |
|---|---|
| C-1 | 语义召回预算：单文件嵌入取前 20K 字符；>200 文件仓库只对名称相关性 top-30 候选嵌入（与 V3.2 关键词召回同一闸门） |
| C-2 | 持久化上限：≤1500 文件 / 每文件 ≤200 符号；损坏/版本不符 JSON 整体弃用（try/catch 静默） |
| C-3 | 语义相关性上限 80 分——永远低于符号精确命中（100），高于关键词（≤50），排序分层语义不被打破 |
| C-4 | @codebase 走共享引擎 + 8K token 预算，与 agent-loop 注入同源同预算 |
| C-5 | 测试通过 `CODEX_CONFIG_PATH` 指向临时目录，绝不污染真实用户 home |

## 四、首席科学家视角

| # | 决策 |
|---|---|
| S-1 | 哈希嵌入的判别力来源：共享 n-gram 数量（词形变体/组合词/中文分词鲁棒性）；L2 归一化后余弦=点积，浮点代价 O(dim)=512 |
| S-2 | 嵌入输入 = 路径 + 文件名 + 内容截断（路径信号防"内容通用、路径才是身份"的错配） |
| S-3 | 增量正确性证明思路：缓存失效以 mtime+size 双指纹为准（同 size 不同 mtime 也失效），宁可多失效不可给过期符号 |

---

## 五、执行清单

1. `src/context/context-engine.ts`：
   - `embedText`/`cosineSimilarity`（导出可测）+ `getFileEmbedding`（LRU）+ `semanticRecall`（公开方法）
   - `assembleContext` 三路 → 四路（符号 100 / 语义 ≤80 / 关键词 ≤50 / 依赖图 5）
   - 持久化：`loadPersistedIndex`（指纹校验种子）+ `queueSave`（异步串行）+ `flushIndexCache`（测试用）
   - `refresh()` 增量刷新 + `LRUCache.delete` + 扫描收集器重构
   - `getSharedContextEngine` 模块级共享单例
2. `src/core/agent-loop.ts`：改用共享单例；每轮 `refresh()`（Agent 的文件编辑下一轮即生效）
3. `codex-ide/src/chat/chat-view-provider.ts`：`@codebase` 检测 → 共享引擎组装 → 上下文注入（隐私红线沿用引擎侧拦截）
4. `tests/context-engine-v34.test.ts`：嵌入归一化/相似度排序、语义召回、持久化跨实例复用、指纹失配弃用、增量刷新（新增/修改/删除）
5. 既有 `tests/context-engine.test.ts` 补 `CODEX_CONFIG_PATH` 隔离（防 home 污染）
6. 验收门：双侧 typecheck + 全量测试 + 双侧打包 → commit + push

## 六、明确不做

- 不做真实 embedding（云端 API 无免费保障；本地 Ollama embedding 列入 V4 可选增强）
- 不做嵌入向量持久化（重建便宜且指纹复杂化，收益不成比例）
- 不做 IDE 侧独立索引器（共享 CLI 引擎即产品宪法的"内核唯一"原则）
