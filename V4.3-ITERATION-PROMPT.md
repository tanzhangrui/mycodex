# V4.3 迭代纲领 — 编排打磨：计划人工确认

> 前情：V4.2 步骤级回滚已落地。本轮与 V4.1 合并交付。
> 核心命题：V4.0 的 /plan 拿到计划直接执行——计划错了就白烧 token 且污染内存 FS。**执行前给用户一次否决权。**

## 范围决策（诚实边界）

- **做**：`onPlanCreated` 确认钩子（编排器生成计划后回调，返回 false → `mode: 'cancelled'` 终止）；CLI 展示计划 + readline y/n 确认
- **不做（明确推迟）**：并行无依赖步骤调度——多步骤共享同一内存 FS 与沙箱，并发写冲突的协调成本超出本轮预算；且廉价模型生成的"无依赖"判断本身不可信（需要文件级冲突分析）。推到 V4.4 或砍掉

## 执行清单

| # | 项 | 文件 |
|---|---|---|
| 1 | `OrchestratorOptions.onPlanCreated`：计划生成后确认回调；false → 返回 cancelled（steps 空、零副作用） | `src/core/orchestrator.ts` |
| 2 | CLI /plan：展示步骤后 `确认执行? (y/n)`；n → 打印取消退出 | `src/cli/text-repl.ts` |
| 3 | 测试：确认 true 正常执行 / false 取消（不执行任何步骤、无编辑） | `tests/orchestrator.test.ts` |

## 验收
双侧 typecheck / 全量测试全绿 / 双侧打包 / CHANGELOG / commit + push（与 V4.1 合并一次交付）
