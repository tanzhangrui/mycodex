# Codex IDE V3.2 迭代纲领 — 上下文引擎：让廉价模型"看得准"

> 前置文档：`AI-IDE-MASTER-PROMPT.md`（产品宪法）、`V3.1-ITERATION-PROMPT.md`（上一轮差距分析）。
> 本文档是 V3.1 → V3.2 的差距分析与执行纲领，延续"产品经理 × 架构师 × CTO × 首席科学家"四视角。

***

## 〇、审视方法

V3.1 交付了信任（检查点回滚）、上下文（@文件引用）、可靠性（失败升档/重试）三大闭环。
本轮聚焦 CHANGELOG 路线图的 V3.2 主题：**上下文引擎**。

核心命题：产品宪法说"模型是商品，编排是壁垒"。廉价/免费模型（GLM-4.7-flash、DeepSeek）
与旗舰模型的差距，很大一部分不在推理，而在**喂给它的上下文质量**。Cursor 的核心壁垒
正是代码库感知（codebase understanding）。本轮把 CLI 内核的上下文从"文件名关键词匹配"
升级为"符号 + 依赖图 + 预算"三引擎。

对照系：Cursor（@codebase 符号检索）、Claude Code（自动相关文件收集）、
Aider（repo-map 符号图）。评判轴：**准**（命中率）、**省**（token 成本）、**快**（索引延迟）。

***

## 一、产品经理视角

| #   | 缺口                                                            | 影响              | 严重度 |
| --- | ------------------------------------------------------------- | --------------- | --- |
| P-1 | 用户提问提到函数/类名时，模型拿不到定义处代码，只能靠工具调用自己找                            | 廉价模型多轮探索=慢+贵+易错 | P0  |
| P-2 | 修改一个文件时，其直接依赖（import 的模块）不自动进入上下文                             | 跨文件改动漏改调用点      | P0  |
| P-3 | 大仓库索引需全量读文件内容，冷启动慢、内存高                                        | 32G 内存机型上大仓体验劣化 | P1  |
| P-4 | `context-engine.ts` 自 V1.4 起标记 @deprecated 且**零集成**——预留模块从未上线 | 路线图承诺未兑现        | P0  |

## 二、架构师视角

| #   | 缺口                                                | 决策                                                                                 |
| --- | ------------------------------------------------- | ---------------------------------------------------------------------------------- |
| A-1 | 现索引在扫描阶段 `readFileSync` 全量文件内容（仅取 500 字符 preview） | 扫描只 stat 不读内容；内容/符号/import 全部按需读取 + LRU                                            |
| A-2 | 无符号概念                                             | 零依赖正则近似 AST：TS/JS/TSX/JSX/Python 的 class/function/method/interface/type/const + 行号 |
| A-3 | 无依赖图                                              | 解析相对 import/require/from 语句 → 文件级有向图，BFS 1\~2 跳扩展                                  |
| A-4 | `ContextEngine` 与 agent-loop 零耦合点                 | `runAgentLoop` 内构建系统提示词时注入 `<project_context>`（带 token 预算）                         |
| A-5 | 引擎独立沙盒                                            | 索引/查询全部 try/catch 非致命——上下文引擎失败绝不阻断 Agent 主循环                                       |

## 三、CTO 视角

| #   | 缺口                                                                                               | 决策                                                             |
| --- | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------- |
| C-1 | **隐私红线违规**：现扫描代码 `entry.startsWith('.') && entry !== '.env'` —— `.env` 被特殊放行，内容前 500 字符直接进入可检索索引 | 复用 `privacy-guard.isSensitivePath`，敏感文件绝不入索引；`.env` 移出文本扩展名白名单 |
| C-2 | 每次会话重建索引                                                                                         | 进程内单例 + workingDir 缓存，仅目录变化时重建                                 |
| C-3 | 大仓符号索引一次性读所有源码                                                                                   | 符号索引惰性构建（首次符号查询触发），单文件 512KB / 总量 3000 文件上限                    |
| C-4 | 上下文注入无限膨胀                                                                                        | 预算裁剪（默认 12K token，agent-loop 注入 8K），相关性排序后截断                   |

## 四、首席科学家视角

| #   | 缺口                         | 决策                                                  |
| --- | -------------------------- | --------------------------------------------------- |
| S-1 | 关键词匹配分不清"文件名碰巧包含"与"真正的定义处" | 符号命中（精确名匹配）相关性 100+，远高于关键词命中（≤10），排序天然分层            |
| S-2 | 单一证据源                      | 三路召回融合：符号定义处 → 关键词最优窗口 → import 图扩展文件头              |
| S-3 | 召回 chunk 边界随机（30 行滑动窗口）    | 符号 chunk 以定义行为中心 ±20 行；import 扩展取文件头 60 行（签名/导出密集区） |

***

## 五、V3.2 执行清单

1. **重写** **`src/context/context-engine.ts`**（去 @deprecated，成为 V3.2 主战场）：

   * `SymbolIndex`：惰性构建、LRU 缓存、`resolveQuerySymbols(query)` 符号名 → 定义文件+行号

   * `ImportGraph`：`parseImports(file)` 惰性解析 + `getRelatedFiles(seeds, maxHops, maxFiles)` BFS

   * 懒加载：扫描零内容读取；`getFileContent`/`getPreview` 按需 + LRU(200)

   * `assembleContext(query, seedFiles?)`：三路召回 + 预算裁剪 + 去重
2. **集成** **`src/core/agent-loop.ts`**：系统提示词注入 `<project_context>`（8K token 预算，失败静默降级）
3. **测试** **`tests/context-engine.test.ts`**：符号定位 / import 扩展 / 懒加载 / 隐私拦截 / 预算裁剪 / 提示词注入
4. **验收门**：typecheck 零错误 + 全量测试绿 + 打包通过 + 敏感扫描零命中 → commit

## 六、明确不做（防过度工程）

* 不引入真实 AST 解析器（tree-sitter/babel）——依赖体积与安装成本违反"轻量内核"宪法，正则近似已覆盖 90% 符号定位场景

* 不做向量/embedding 检索——免费 API 无 embedding 保障，语义检索列入 V4

* 不做多会话索引持久化——进程内缓存已够 CLI 生命周期

